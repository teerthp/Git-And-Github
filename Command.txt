<file_name.file_extensions> = file name with extensions
(number) = any number at that place
== = then
branch_name = name of the new branch
<ssh_url> = ssh url of github

ls
pwd
ls -lart (To show Hidden Files)
clear
touch <file_name.file_extensions>

git config --global user.name Teerth
git config --global user.name
git config --global user.email teerthpathak8@gmail.com
git config --global user.email

git init
touch <file_name.file_extensions>
git status
git add <file_name.file_extensions>
git status
git commit == Initial commit
git status
git add -A
git status
git commit -a -m "Added more htmls"
git status

git checkout <file_name.file_extensions>
git status
git checkout -f
git status

git log
git log -p -(number)

git diff
git status
git diff --staged
git status

git commit -a -m "skipped staging area and fixes <"
git log

git rm <file_name.file_extensions>
git status
git rm --cached <file_name.file_extensions>
git status

git status -s

touch .gitignore
touch mylogs.log

git branch branch_name
git status
git branch
git checkout branch_name
git status
git checkout master
git status

git merge teerth

git checkout -b teerthpathak (Will Create A New Branch And Take You In It)
git branch
git status


//////////////////////////////////////////////////////////////////////////////////////////////////////////////////

git remote add origin https://github.com/teerthpathak/Git.git
git remote
git remote -v
git push origin master

ssh-keygen -t rsa -b 4096 -C "teerth@teerthpathak8@gmail.com"
eval $(ssh-agent -s)
ssh-add <file_name.file_extensions>
cat ssh-add <file_name.file_extensions>

git remote set-url origin <ssh_url>
git remote
git remote -v
git push -u origin master